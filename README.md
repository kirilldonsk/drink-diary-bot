# Telegram-бот: Дневник рецептов напитков

Бот для ведения технологического дневника по каждому напитку:
- отдельные напитки (сидр, медовуха и т.д.);
- записи по датам с историей изменений;
- улучшение текста записи через GPT (ProxyAPI);
- генерация QR в формате SVG;
- публичные страницы по QR (`/q/:token`), включая подарочные именные бутылки.

## Что реализовано

- `Telegram bot` на `grammy` с меню:
  - `➕ Новый напиток`
  - `📂 Текущие напитки`
  - `🗄 Архивные напитки`
  - `🔗 QR напитка`
  - `💾 Бэкап CSV`
- `SQLite` база (`better-sqlite3`) с таблицами:
  - `users`
  - `drinks`
  - `journal_entries`
  - `share_links`
  - `user_states`
- `Fastify` веб-сервер:
  - `GET /health`
  - `GET /q/:token` (страница напитка или подарочной бутылки)
- Интеграция ProxyAPI через OpenAI-compatible SDK:
  - base URL по умолчанию: `https://openai.api.proxyapi.ru/v1`
  - модель задается через `PROXYAPI_MODEL`
  - при генерации QR бот дополнительно пытается мягко откорректировать записи (без рекомендаций и без добавления новых фактов)
- CSV-бэкап в Telegram:
  - ручной экспорт по кнопке;
  - автоотправка в чат по умолчанию: раз в 7 дней (можно переключить на 14/30 дней или выключить).

## Требования

- Node.js 20+
- Telegram bot token (через [@BotFather](https://t.me/BotFather))

## Быстрый старт

1. Установить зависимости:

```bash
npm install
```

2. Создать `.env` из примера:

```bash
cp .env.example .env
```

3. Заполнить `.env`:

```env
TELEGRAM_BOT_TOKEN=<YOUR_TELEGRAM_BOT_TOKEN>
PUBLIC_BASE_URL=https://drinks.example.com
PORT=3000
DB_PATH=./data/drinks.sqlite

PROXYAPI_KEY=<YOUR_PROXYAPI_KEY>
PROXYAPI_BASE_URL=https://openai.api.proxyapi.ru/v1
PROXYAPI_MODEL=openai/gpt-4o-mini
```

4. Запуск в dev-режиме:

```bash
npm run dev
```

Проверка типов:

```bash
npm run check
```

Сборка:

```bash
npm run build
npm start
```

## Продакшн через Docker (q.kzim.ru)

Ниже готовый сценарий под ваш сервер `91.149.241.43` и домен `q.kzim.ru`.

### 1. DNS

В панели DNS создайте/проверьте `A` запись:
- `q.kzim.ru -> 91.149.241.43`

### 2. Подготовить Git-репозиторий

Если проекта еще нет в удаленном Git:

```bash
git init
git add .
git commit -m "Initial bot version"
git branch -M main
git remote add origin <YOUR_GIT_REPO_URL>
git push -u origin main
```

### 3. Установить Docker на сервере

Подключитесь к серверу:

```bash
ssh root@91.149.241.43
```

Установка Docker + compose plugin:

```bash
apt update
apt install -y ca-certificates curl gnupg
install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
chmod a+r /etc/apt/keyrings/docker.asc
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu \
  $(. /etc/os-release && echo \"$VERSION_CODENAME\") stable" | \
  tee /etc/apt/sources.list.d/docker.list > /dev/null
apt update
apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# если используете UFW
ufw allow OpenSSH
ufw allow 80/tcp
ufw allow 443/tcp
ufw --force enable
```

### 4. Клонировать проект и заполнить `.env`

```bash
mkdir -p /opt/drink-bot
cd /opt/drink-bot
git clone <YOUR_GIT_REPO_URL> app
cd app
cp .env.server.example .env
```

Откройте `.env` и заполните:
- `TELEGRAM_BOT_TOKEN`
- `PROXYAPI_KEY` (если используете GPT)

Остальное уже готово для домена `q.kzim.ru`:
- `PUBLIC_BASE_URL=https://q.kzim.ru`
- `DB_PATH=/data/drinks.sqlite`
- `PORT=3000`

### 5. Запустить контейнеры

```bash
docker compose up -d --build
docker compose ps
docker compose logs -f app
```

Контейнеры:
- `app` (бот + HTTP сервер)
- `caddy` (reverse proxy + автоматический HTTPS)

### 6. Проверка

```bash
curl http://127.0.0.1:3000/health
curl https://q.kzim.ru/health
```

Если DNS уже обновился и порты `80/443` открыты, Caddy сам выдаст SSL-сертификат.

### 7. Обновление через Git

```bash
cd /opt/drink-bot/app
git pull
docker compose up -d --build
docker compose logs -f app
```

### 8. Команды эксплуатации

```bash
docker compose restart app
docker compose logs -f
docker compose down
docker compose up -d
```

### 9. Важные замечания

- Не меняйте `DB_PATH` на путь вне `/data`, иначе потеряете сохранение в Docker volume.
- Бэкап CSV в Telegram уже встроен в бота и работает как удаленный резерв.
- Для дополнительного оффлайн-бэкапа SQLite можно периодически копировать `/data/drinks.sqlite`.

## Тестовый сайт без домена

Для QR-ссылок нужен публичный URL. На тесте проще всего поднять локально и пробросить через туннель.

### Вариант A: Cloudflare Tunnel (без регистрации)

1. Установите `cloudflared`:

```bash
brew install cloudflared
```

2. В `.env` временно поставьте:

```env
PUBLIC_BASE_URL=http://localhost:3000
```

3. Запустите проект:

```bash
npm run dev
```

4. Во втором терминале запустите туннель:

```bash
cloudflared tunnel --url http://localhost:3000
```

5. `cloudflared` выдаст URL вида:
- `https://random-name.trycloudflare.com`

6. Обновите `.env`:

```env
PUBLIC_BASE_URL=https://random-name.trycloudflare.com
```

7. Перезапустите `npm run dev`.

После этого бот будет генерировать QR уже на тестовый публичный сайт.

### Вариант B: ngrok

1. Установите и авторизуйте `ngrok` (по инструкции ngrok).
2. Запустите проект:

```bash
npm run dev
```

3. Во втором терминале:

```bash
ngrok http 3000
```

4. Возьмите `https` URL от ngrok и поставьте его в `PUBLIC_BASE_URL`, затем перезапустите проект.

## Как пользоваться ботом

1. Нажмите `➕ Новый напиток` и создайте, например, `Сидр`.
2. Нажмите `📂 Текущие напитки`, выберите напиток и действие:
   - `📝 Добавить запись` (для текущих)
   - `📦 Архивировать` (перенос в архив)
3. В записи отправьте:
   - `2026-02-22 | Снял с осадка, добавил 50 г меда, плотность 1.048`
   - или просто текст (дата будет сегодняшняя).
4. Нажмите `🗄 Архивные напитки`, выберите напиток и `♻️ Вернуть в текущие`.
5. Нажмите `🔗 QR напитка`, затем:
   - выберите тип: `Обычный QR` или `Подарочный QR`;
   - выберите напиток.
6. Для подарочного QR:
   - введите `Имя получателя`;
   - номер бутылки присваивается автоматически;
   - выберите: без сообщения или добавить сообщение.
7. Нажмите `💾 Бэкап CSV`:
   - `📤 Экспорт сейчас` отправит полный CSV с напитками, записями и QR-ссылками;
   - выберите частоту автоотправки (`7 дней`, `14 дней`, `30 дней` или `Выкл`).

## Архитектура проекта

```text
src/
  bot.ts            # Telegram сценарии и меню
  server.ts         # HTTP сервер и публичные страницы
  db.ts             # SQLite + миграции
  config.ts         # Загрузка ENV
  index.ts          # Точка запуска
  services/
    llm.ts          # ProxyAPI/GPT улучшение текста
    qr.ts           # Генерация QR SVG
    backup.ts       # Сборка CSV-бэкапа
    backupScheduler.ts # Автоотправка бэкапа по расписанию
    render.ts       # HTML рендер страницы по QR
  lib/
    parsers.ts      # Парсинг даты записи
    format.ts       # Вспомогательные функции
Dockerfile          # Образ приложения
docker-compose.yml  # app + caddy
deploy/Caddyfile    # HTTPS + reverse proxy для q.kzim.ru
*.env*.example      # Шаблоны переменных окружения
```

## Публичные ссылки и QR

- Для напитка создается уникальный токен и ссылка вида:
  - `https://your-domain.com/q/<token>`
- Для подарка создается отдельный токен (имя, номер бутылки, сообщение хранятся в БД).
- QR отправляется в Telegram именно как `.svg` документ.

## Источник по ProxyAPI

- [ProxyAPI Overview](https://proxyapi.ru/docs/overview)
